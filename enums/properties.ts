export enum Component {
  CHECKBOX,
  SELECT,
  TEXT,
  SLIDER,
  NUMBER_SELECTOR,
  SWITCH,
  COLOR_PICKER,
}

export enum UI {
  CHECKBOX,
  SELECT,
  TEXT,
  SLIDER,
  NUMBER_SELECTOR,
  SWITCH,
  COLOR_PICKER,
}

export enum PropertyGroup {
  STYLE,
  CONTENT,
  SCROLL_BEHAVIOR,
  BASIC,
  POSITIONING,
}

export enum PropertyType {
  PROP,
  SLOT,
}
